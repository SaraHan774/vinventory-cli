// Frontend 모듈: React + Vite 앱
// 이 파일은 Gradle에서 frontend를 인식하기 위한 더미 파일입니다.
// 실제 빌드는 npm/yarn을 통해 이루어집니다.

// 프론트엔드는 Node.js 기반이므로 Kotlin/Gradle 설정이 필요 없습니다.
// Vite를 사용한 빌드는 frontend/ 디렉토리에서 npm 명령으로 수행됩니다.

